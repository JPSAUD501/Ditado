[package]
name = "ditado_native_automation"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
enigo = "0.6.1"
napi = { version = "3.3.0", default-features = false, features = ["napi8"] }
napi-derive = "3.2.5"

[build-dependencies]
napi-build = "2.3.0"
